export enum Tracks {
    regular = 'REGULAR',
    vip = 'VIP',
};

export enum Vehicles {
    bike = 'BIKE',
    car = 'CAR',
    suv = 'SUV'
};

export enum Outputs {
    success = 'SUCCESS',
    raceTrackFull = 'RACETRACK_FULL',
    invalidEntryTime = 'INVALID_ENTRY_TIME',
    invalidExitTime = 'INVALID_EXIT_TIME',
};
